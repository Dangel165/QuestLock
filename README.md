# QuestLock v1.0.0

**Created by Dangel**

RSA-4096 암호화를 사용하여 파일을 암호화하고, 3가지 게임을 모두 클리어하면 복호화됩니다.

## ⚠️ 경고

**파일이 실제로 암호화됩니다. 3가지 게임을 모두 완료해야만 복구할 수 있습니다.**

## 주요 기능

### 🔒 암호화 시스템
- **RSA-4096** 암호화 사용
- 하이브리드 암호화 (큰 파일은 AES-256 + RSA)
- **173개 파일 형식 지원** (문서, 이미지, 비디오, 오디오, 코드 등)
- 시스템 폴더 및 시스템 파일 자동 보호

### 🎮 게임 시스템
**3가지 게임을 모두 클리어해야 복호화 가능:**

1. **비트맵 맞추기**: 섞인 타일을 올바른 위치에 배치
2. **ASCII 코드 퍼즐**: 4글자 단어의 ASCII 코드 입력
3. **컴퓨터 지식 퀴즈**: 컴퓨터/프로그래밍 관련 문제 풀이 (힌트 제공)

**난이도 자동 조정** (파일 개수 기반):
- 1-10개 파일: 쉬움 (EASY)
- 11-50개 파일: 보통 (MEDIUM)
- 51개 이상: 어려움 (HARD)

### 📁 지원 파일 형식 (173개)

#### 문서 (18개)
`.txt`, `.doc`, `.docx`, `.pdf`, `.xls`, `.xlsx`, `.ppt`, `.pptx`, `.hwp`, `.hwpx`, `.hwt` 등

#### 이미지 (18개)
`.jpg`, `.png`, `.gif`, `.bmp`, `.svg`, `.psd`, `.raw` 등

#### 비디오 (14개)
`.mp4`, `.avi`, `.mkv`, `.mov`, `.wmv`, `.flv` 등

#### 오디오 (13개)
`.mp3`, `.wav`, `.flac`, `.aac`, `.ogg`, `.wma` 등

#### 코드 (84개)
`.py`, `.java`, `.cpp`, `.js`, `.ts`, `.html`, `.css`, `.jsx`, `.tsx`, `.vue`, `.dart`, `.md` 등

#### 기타
압축 파일, 데이터베이스, 백업 파일 등

### 🛡️ 시스템 보호

#### 자동 차단되는 항목
- **시스템 폴더**: Windows, Program Files, AppData 등
- **시스템 파일**: .exe, .dll, .sys 등 21개 확장자
- **중요 파일**: bootmgr, ntldr, pagefile.sys 등

#### 보호 기능
- 시스템 폴더 선택 시 경고 창 자동 표시
- 시스템 파일 자동 필터링
- 컴퓨터 안전성 보장

## 설치 및 실행

### 요구사항
- Python 3.8 이상
- Windows, macOS, Linux 지원

### 의존성 설치
```bash
pip install -r requirements.txt
```

**필수 라이브러리:**
- cryptography (RSA-4096 암호화)
- Pillow (이미지 처리)

### 실행 방법

#### Windows
```bash
run_questlock.bat
```

#### Python 직접 실행
```bash
python game_ransomware_simulator/main.py
```

또는

```bash
python -m game_ransomware_simulator.main
```

## 사용 방법

### 1단계: 프로그램 시작
- 면책조항 확인 및 동의
- 경고 메시지 숙지

### 2단계: 폴더 선택
- "📂 폴더 선택" 버튼 클릭
- 암호화할 폴더 선택
- **주의**: 테스트용 폴더만 선택!

### 3단계: 폴더 정보 확인
- 파일 개수 확인
- 총 크기 확인
- 게임 난이도 확인

### 4단계: 암호화 시작
- "🔐 암호화 시작" 버튼 클릭
- 최종 확인 메시지 확인
- 암호화 진행 (초록색 진행 바)

### 5단계: 게임 플레이
**3가지 게임을 순서대로 클리어:**
1. 비트맵 맞추기
2. ASCII 코드 퍼즐
3. 컴퓨터 지식 퀴즈

### 6단계: 복호화
- 3가지 게임 모두 완료 시 자동 복호화
- 복호화 진행 (주황색 진행 바)
- 파일 복구 완료

### 7단계: 파일 복구 (선택사항)
- 복호화 실패 파일이 있는 경우
- "🔧 파일 복구" 버튼 사용
- 개별 파일 복구 가능

## 게임 상세 설명

### 🎨 비트맵 맞추기
- 섞인 타일을 클릭하여 위치 교환
- 모든 타일을 올바른 위치에 배치
- 난이도별 그리드 크기:
  - EASY: 3x3
  - MEDIUM: 4x4
  - HARD: 5x5

### 🔤 ASCII 코드 퍼즐
- 4글자 단어가 주어짐
- 각 문자의 ASCII 코드 입력
- 모든 코드가 정확해야 완료

### 💻 컴퓨터 지식 퀴즈
- 컴퓨터/프로그래밍 관련 문제 출제
- 30개 문제 (난이도별 10개씩)
- 힌트 기능 제공 (💡 힌트 보기)
- 대소문자 구분 없음

**문제 예시:**
- EASY: "HTTP의 기본 포트 번호는?"
- MEDIUM: "SQL Injection 공격을 방어하는 기법은?"
- HARD: "AES-256의 블록 크기는 몇 비트인가?"

## 기술적 세부사항

### 암호화 방식
- **RSA-4096**: 공개키 암호화
- **AES-256**: 대칭키 암호화 (큰 파일용)
- **하이브리드**: 파일 크기에 따라 자동 선택

### 파일 구조
```
game_ransomware_simulator/
├── core/               # 핵심 로직
│   ├── file_manager.py      # 파일 관리
│   ├── simulator_controller.py  # 메인 컨트롤러
│   └── models.py        # 데이터 모델
├── crypto/             # 암호화
│   └── crypto_manager.py    # RSA/AES 암호화
├── games/              # 게임 로직
│   ├── bitmap_game.py
│   ├── ascii_game.py
│   └── riddle_game.py
├── ui/                 # GUI
│   ├── main_window.py       # 메인 윈도우
│   └── game_windows.py      # 게임 윈도우
└── main.py             # 진입점
```

### 키 저장
- 키 저장 위치: `~/.game_ransomware/keys/`
- 세션 ID 기반 키 관리
- 복호화 시 자동 키 로드

## 안전 사용 가이드

### ✅ 권장사항
1. **테스트 폴더 생성**: 중요하지 않은 파일로 테스트
2. **백업 확인**: 중요 파일은 백업 후 테스트
3. **가상 머신**: 가능하면 VM 환경에서 테스트
4. **소량 테스트**: 처음에는 적은 파일로 시작

### ⚠️ 주의사항
1. **시스템 폴더 금지**: Windows, Program Files 등 선택 금지
2. **중요 데이터 금지**: 복구 불가능한 데이터 사용 금지
3. **게임 클리어 필수**: 게임을 못 풀면 복구 불가
4. **교육 목적**: 교육 및 학습 목적으로만 사용

### ❌ 절대 금지
- 타인의 컴퓨터에 무단 사용
- 불법적인 목적으로 사용
- 시스템 폴더 암호화 시도
- 백업 없는 중요 데이터 암호화

## 문제 해결

### 암호화가 안 되는 경우
- 시스템 폴더인지 확인
- 파일 권한 확인
- 지원되는 파일 형식인지 확인

### 복호화가 안 되는 경우
- 3가지 게임을 모두 클리어했는지 확인
- "🔧 파일 복구" 버튼 사용
- 키 파일 존재 확인 (`~/.game_ransomware/keys/`)

### 게임이 안 열리는 경우
- Python 버전 확인 (3.8 이상)
- 의존성 재설치: `pip install -r requirements.txt`
- Tkinter 설치 확인

## 면책 조항

본 프로그램은 **교육 목적**으로만 제작되었습니다.

- 프로그램 사용으로 인한 모든 데이터 손실은 사용자 책임입니다
- 개발자는 어떠한 법적 책임도 지지 않습니다
- 불법적인 목적으로 사용할 수 없습니다
- 타인의 컴퓨터에 무단으로 사용할 수 없습니다

## 라이선스

교육 목적으로만 사용 가능합니다.

## 제작자

**Dangel**

## 버전

**v1.0.0**


